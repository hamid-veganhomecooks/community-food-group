---
import BaseLayout from '../layouts/BaseLayout.astro';
import locations from '../data/locations.json';
import { MapPin, Clock, Phone, Mail } from 'lucide-astro';
---

<BaseLayout
  title="Locations"
  description="Find a community food group location near you"
  pageTitle="Our Locations"
>
  <div class="section bg-cream">
    <div class="container">
      <div class="max-w-3xl mx-auto text-center mb-stack">
        <h1 class="text-title font-bold text-terracotta-dark">
          Find Us
        </h1>
        <p class="text-lead text-gray-600 max-w-lede mx-auto mt-stack-sm">
          Visit any of our locations to get involved, pick up food, or learn more about our programs.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-stack">
        {locations.map((location) => (
          <article id={location.id} class="card">
            <h2 class="text-subheading font-bold text-terracotta mb-stack-sm">
              {location.name}
            </h2>

            <div class="space-y-4 text-gray-700">
              <div class="flex items-start space-x-2">
                <MapPin class="w-5 h-5 text-terracotta shrink-0 mt-0.5" aria-hidden="true" />
                <div>
                  <p>{location.address}</p>
                  <a
                    href={`https://www.google.com/maps?q=${location.coordinates.lat},${location.coordinates.lng}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-label text-terracotta hover:text-terracotta-dark"
                    aria-label={`Get directions to ${location.name}`}
                  >
                    Get Directions →
                  </a>
                </div>
              </div>

              <div class="flex items-start space-x-2">
                <Clock class="w-5 h-5 text-terracotta shrink-0 mt-0.5" aria-hidden="true" />
                <div>
                  <p class="font-medium text-gray-800">Hours:</p>
                  <ul class="text-label" aria-label="Operating hours">
                    {Object.entries(location.hours).map(([day, hours]) => (
                      <li class="flex justify-between gap-4">
                        <span class="capitalize">{day}</span>
                        <span class="text-gray-600">{hours}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>

              {location.phone && (
                <div class="flex items-center space-x-2">
                  <Phone class="w-5 h-5 text-terracotta shrink-0" aria-hidden="true" />
                  <a href={`tel:${location.phone}`} class="hover:text-terracotta">
                    {location.phone}
                  </a>
                </div>
              )}

              {location.email && (
                <div class="flex items-center space-x-2">
                  <Mail class="w-5 h-5 text-terracotta shrink-0" aria-hidden="true" />
                  <a href={`mailto:${location.email}`} class="hover:text-terracotta">
                    {location.email}
                  </a>
                </div>
              )}
            </div>

            {location.features && (
              <div class="mt-stack-sm">
                <h3 class="text-label font-semibold text-gray-800 mb-2">Features:</h3>
                <div class="flex flex-wrap gap-2">
                  {location.features.map((feature) => (
                    <span
                      class="bg-sage-light/20 text-sage-dark px-3 py-1 rounded-full text-label"
                    >
                      {feature}
                    </span>
                  ))}
                </div>
              </div>
            )}

            <p class="mt-stack-sm text-gray-700">
              {location.description}
            </p>
          </article>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>